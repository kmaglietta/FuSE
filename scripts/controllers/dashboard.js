'use strict';

angular.module('tossApp')
  .controller('DashboardCtrl', ['$scope','$log', function ($scope, $log) {
    var ctrl = this;

    ctrl.logout = function(){
      // Log user out
    }
  }]);
